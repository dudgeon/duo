# v0.6.10 walk carry-over — items shipped without a walk

> Owner directive 2026-05-09 at cut time: *"Cut — but preserve checklist for next smoke walk."*
>
> Sprint 12 walk-rev4 PASSED for ENH-108 markdown paste-image + ENH-111 image viewer (in normal-pane layout). The cut went out before the canvas mirror + CLI verb additions could get their own walk — Geoff opted for "A) Cut now on faith" once the markdown-side variants passed. These items inherit the same architecture as the walked variants but should be exercised in the next sprint's smoke walk before being treated as stable.

## Items to include in the next smoke-walk manifest

### ENH-108 canvas paste-image (Sprint 12)

**Status at cut:** typecheck clean, architecture-mirrors the markdown editor's handlePaste/handleDrop (which PASSED walk-rev4). Canvas calls `installPagePasteHandlers(doc, { activeDocPath: path })`; image branch uses `files.saveImageBeside` + blob URL + `doc.execCommand('insertHTML', false, '<img src="blob:..." alt="">')`.

**Walk steps:**
1. Open any HTML canvas tab — `duo edit /tmp/canvas-test.html` or via FileTree.
2. Click into the canvas body. ⌘V an image from clipboard. Confirm: image renders inline.
3. Drag-drop an image from Finder onto the canvas body. Confirm: image renders inline + copy lands at `dirname(canvas-html)/image-<stamp>-<hash>.<ext>`.
4. Verify HTML source: `cat /tmp/canvas-test.html` — should show `<img src="blob:http://localhost:5173/..." alt="">`. Same v1 trade-off as markdown editor (FOLLOWUP-014): blob URLs don't survive reload.

### ENH-108 CLI: `duo image insert <path> [--alt "…"]` (Sprint 12)

**Status at cut:** self-tested live by local Claude — `duo image insert /tmp/enh108-test/image-...png --alt "from cli"` returned `{absPath: "/tmp/enh108-test/image-<stamp>-<hash>.png"}` and the file landed on disk. v1 markdown-editor target only.

**Walk steps:**
1. Open a markdown file: `duo edit /tmp/walk-cli.md` (create with `printf '# CLI test\n' > /tmp/walk-cli.md`).
2. **CRITICAL — click the tab to make it active** (BUG-101 — `duo edit` opens but doesn't focus).
3. From any terminal: `duo image insert /tmp/some-source-image.png --alt "test"`. Confirm: returns `{absPath: ...}` JSON. Image renders inline in the editor at caret position. New `image-<stamp>-<hash>.png` file lands beside `walk-cli.md`.
4. Failure-mode test: `duo image insert /tmp/walk-cli.md` (a markdown file, not an image) — should error: `image insert: unsupported extension .md (supported: png, jpg, jpeg, gif, webp, svg, bmp, tiff)`.
5. Failure-mode test 2: `duo image insert /nonexistent/path.png` — should error: `ENOENT: no such file or directory`.

### ENH-121 renderer console forwarder (Sprint 12, dev-tooling)

**Status at cut:** verified live — opening an image triggered `[renderer:warn] (App.tsx:466) [BUG-101 openFile] [object Object]` to dev stdout. No walk needed, but include a regression check.

**Walk steps:**
1. In dev (`npm run dev`), tail dev stdout (or check the bg-task log).
2. Open Duo's DevTools (Cmd+Opt+I), Console tab. Run `console.log('forwarder test')`.
3. Confirm dev stdout shows `[renderer:log] (...) forwarder test`.
4. Run `console.warn('test warn')` and `console.error('test err')`. Confirm both appear with `[renderer:warn]` / `[renderer:error]` prefixes.

## Won't-be-walked but worth a sanity check

- **BUG-110 smoke-walk localStorage collision fix** — verified by THIS sprint's three rev manifests not colliding (rev2/rev3/rev4 each got fresh state). Won't regress unless `.claude/skills/smoke-walk/generate.mjs` line 100 is changed.
- **CSP `img-src` update** — verified by walk-rev4. Won't regress unless `renderer/index.html` is reverted.
- **`duo-asset://` custom protocol** — kept as fallback path even though the actual fix used blob URLs. Verifiable via `duo open file:///tmp/duo-asset-test.html` (test page kept at `/tmp/enh108-test/duo-asset-test.html` from this sprint's debugging).

## Notes for next-sprint planner

- After the walk, file any failures as bugs against the v0.6.10 release.
- The canvas paste-image inherits the FOLLOWUP-014 (blob-URL non-portable) trade-off. If the v2 NodeView lands as a Sprint 13 anchor, this checklist's first item gets re-walked then.
- Once these items pass a real walk, this file can be deleted (or moved to `docs/dev/walk-carryovers-archive/`).

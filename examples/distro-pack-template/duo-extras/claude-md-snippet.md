## Example Distro guidance

This is a placeholder snippet — replace it with your organization's
Duo-aware guidance for Claude. The snippet ships into the user's
`~/.claude/CLAUDE.md` between distro-managed markers (see Stage 21d
PRD § "Multi-distro CLAUDE.md merge").

Suggested content:
- A pointer to your team's playbook / wiki.
- Conventions for which skills / agents are organization-canonical
  (so Claude knows when to reach for them).
- A note about which `~/.claude/duo/distros/<distro>/` folders contain
  org-specific canvases / playgrounds Claude can `duo open` directly.
